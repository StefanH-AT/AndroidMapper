<resources>

    <!-- Light base application theme -->
    <style name="AppTheme.Light" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/lightPrimary</item>
        <item name="colorPrimaryDark">@color/lightPrimaryDark</item>
        <item name="colorAccent">@color/lightAccent</item>
        <item name="android:actionBarWidgetTheme">@style/Widget.AppCompat.ActionBar.Solid</item>

    </style>

    <!-- Dark base application theme -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/darkPrimary</item>
        <item name="colorPrimaryDark">@color/darkPrimaryDark</item>
        <item name="colorAccent">@color/darkAccent</item>
    </style>

    <style name="AppTheme.Light.Fullscreen" parent="AppTheme.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.Dark.Fullscreen" parent="AppTheme.Dark">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    
    <!-- Preferences Styles -->
    <style name="SettingsImportant">
        <item name="android:drawableEnd">@drawable/ic_star_white_12dp</item>
        <item name="android:drawablePadding">@dimen/text_icon_padding</item>
        <item name="android:drawableTint">?attr/colorAccent</item>
        <item name="android:layout_width">wrap_content</item>
    </style>
    
    
</resources>
